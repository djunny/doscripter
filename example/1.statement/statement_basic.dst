#language basic
Dim i = 10
if i = 1 then
  alert("i=1")
else 
  for i=0 to 10
    select case i
      case 1: echo(i+1)
      case 2..10: echo(i)
      case else: echo(i+10)
    end select
  next
  do 
    echo(i)
    i = i + 1
  loop while i<20
end if